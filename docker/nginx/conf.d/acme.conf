# ACME HTTP-01 challenge handler (included in the port-80 server block).
# Certbot writes challenge tokens to the shared certbot_www volume.
location ^~ /.well-known/acme-challenge/ {
    root /var/www/certbot;
    default_type "text/plain";
    try_files $uri =404;
}
