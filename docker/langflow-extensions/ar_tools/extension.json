{
  "$schema": "https://schemas.langflow.org/extension/v1.json",
  "id": "ar-tools",
  "version": "0.1.0",
  "name": "AR Tools",
  "description": "Accounts-receivable source-system tools (Zoho Books invoices/customers/customer payments + FOODICS POS receipts/sales) for the Cosmic AR Agent. Packaged as an installed Extension Bundle so the components are server-trusted (recognized on the public playground path) rather than inline custom code. Credentials are supplied via LangFlow Secret Global Variables selected on each component (load_from_db).",
  "lfx": {
    "compat": ["1"]
  },
  "bundles": [
    {
      "name": "ar_tools",
      "path": "components/ar_tools",
      "display_name": "AR Tools",
      "icon": "BookOpen"
    }
  ],
  "capabilities": {
    "requiresCredentials": true
  }
}